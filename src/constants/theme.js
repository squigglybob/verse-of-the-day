import { createMuiTheme } from '@material-ui/core'

const themeOverides = createMuiTheme({
    palette: {
        primary: {
            main: '#009A94',
        }
    }
})

export default themeOverides